# MeetMind MVP 差距分析与架构优化

> 生成时间: 2026-01-06

---

## 一、MVP 需求对照表

### 1.1 核心功能完成度

| 模块 | MVP 需求 | 当前状态 | 完成度 | 问题 |
|------|----------|----------|--------|------|
| **Capture 采集** | 通义听悟实时转写 | ⚠️ 部分 | 60% | 依赖外部 Discussion 服务，无独立运行能力 |
| **Anchor 断点** | 一键标注 + 5秒撤销 | ✅ 完成 | 100% | - |
| **Memory 时间轴** | 转录 + 断点整合 | ✅ 完成 | 90% | 缺少音频回放功能 |
| **Tutor AI家教** | 证据链问答 | ✅ 完成 | 85% | 引用匹配精度可优化 |
| **学生端** | 录音+复习三栏布局 | ✅ 完成 | 90% | 缺少音频播放器 |
| **家长端** | 日报+陪学脚本 | ✅ 完成 | 80% | 数据来源是 Mock |
| **教师端** | 困惑热区+反思 | ❌ 未实现 | 0% | 完全缺失 |

### 1.2 三个项目复用情况

| 项目 | 计划复用能力 | 实际复用 | 复用度 |
|------|--------------|----------|--------|
| **Discussion** | 通义听悟、通义千问 | ✅ LLM 直接调用 + ⚠️ 听悟需启动后端 | 70% |
| **LongCut** | 时间轴UI、引用匹配、翻译 | ✅ 工具函数复制 | 60% |
| **Open Notebook** | 向量搜索、多模型、笔记 | ⚠️ API 调用（需启动服务） | 40% |

---

## 二、架构问题分析

### 2.1 🔴 严重问题

#### 问题 1: 服务依赖过重
```
当前架构:
MeetMind (3001) 
    ├── 依赖 Discussion (4000) - 通义听悟
    ├── 依赖 Open Notebook (5055) - 向量搜索
    └── 直接调用 DashScope API - LLM

问题: 需要启动 3 个服务才能完整运行
```

**建议**: 将核心能力内置，外部服务作为增强

#### 问题 2: 数据持久化缺失
```
当前: 全部使用 localStorage
问题:
- 数据无法跨设备同步
- 无法支持多用户
- 数据量大时性能问题
- 刷新可能丢失
```

**建议**: 添加后端 API + 数据库（PostgreSQL/SQLite）

#### 问题 3: 无真实音频处理
```
当前: 
- Recorder 组件录音但不保存
- 无音频回放功能
- 无音频存储

MVP 需求:
- 音频存储（OSS）
- 片段回放（引用跳转）
```

**建议**: 添加音频存储和播放器组件

### 2.2 🟡 中等问题

#### 问题 4: 通义听悟集成不完整
```
当前: 
- 需要 Discussion 后端运行
- WebSocket 连接不稳定
- 无降级方案

建议:
- 直接集成通义听悟 SDK
- 或添加本地 Whisper 降级
```

#### 问题 5: 教师端完全缺失
```
MVP 需求:
- 困惑热区可视化
- AI 生成课后反思
- 班级数据统计

当前: 0% 实现
```

#### 问题 6: 环境变量管理混乱
```
当前:
- .env.local 包含真实 API Key（安全风险）
- 客户端直接访问环境变量
- 无配置验证

建议:
- 服务端处理敏感配置
- 添加配置验证启动检查
```

### 2.3 🟢 轻微问题

#### 问题 7: Mock 数据混杂
```
文件: src/app/page.tsx
问题: MOCK_SEGMENTS 硬编码在组件中
建议: 分离到 fixtures 目录
```

#### 问题 8: 类型定义分散
```
问题: 
- Breakpoint vs Anchor 类型重复
- 多处定义 Segment 接口
建议: 统一到 types/ 目录
```

#### 问题 9: 服务状态指示器无实际作用
```
Header 组件显示服务状态
但: 
- 不影响功能降级
- 用户不知道如何处理
```

---

## 三、TODO LIST（按优先级排序）

### P0 - 阻塞性问题（必须修复）

- [ ] **T01** 添加后端 API 层（Next.js API Routes 扩展）
  - `/api/sessions` - 会话管理
  - `/api/anchors` - 断点 CRUD
  - `/api/timelines` - 时间轴存储
  
- [ ] **T02** 添加数据库支持
  - 选择: SQLite（本地）或 PostgreSQL（生产）
  - Prisma ORM 集成
  
- [ ] **T03** 通义听悟直接集成
  - 不依赖 Discussion 后端
  - 添加本地 Web Speech API 降级

### P1 - 核心功能补全

- [ ] **T04** 教师端页面 `/teacher`
  - 困惑热区可视化（复用 memoryService.getConfusionHotspots）
  - AI 生成课后反思
  - 班级统计面板
  
- [ ] **T05** 音频处理
  - 录音保存（IndexedDB 或 OSS）
  - 音频播放器组件
  - 时间轴点击跳转播放
  
- [ ] **T06** 数据同步
  - 学生端 → 家长端数据流
  - 实时更新（WebSocket 或轮询）

### P2 - 体验优化

- [ ] **T07** 统一类型定义
  - 创建 `src/types/` 目录
  - 合并 Anchor/Breakpoint
  - 统一 Segment 定义
  
- [ ] **T08** 分离 Mock 数据
  - 创建 `src/fixtures/` 目录
  - 开发模式自动加载
  
- [ ] **T09** 配置验证
  - 启动时检查必要环境变量
  - 缺失时显示友好提示
  
- [ ] **T10** 服务降级策略
  - Open Notebook 不可用 → 本地搜索
  - Discussion 不可用 → Web Speech API
  - 显示降级状态提示

### P3 - 生产就绪

- [ ] **T11** 用户认证
  - 学生/家长/教师角色
  - 简单密码或 OAuth
  
- [ ] **T12** 部署配置
  - Docker Compose 整合
  - 环境变量模板
  
- [ ] **T13** 监控埋点
  - 验收指标数据采集
  - 错误上报

---

## 四、推荐架构改进

### 4.1 目标架构

```
┌─────────────────────────────────────────────────────────────┐
│                     MeetMind (Next.js)                       │
├─────────────────────────────────────────────────────────────┤
│  Pages                                                       │
│  ├── / (学生端)                                              │
│  ├── /parent (家长端)                                        │
│  └── /teacher (教师端) [TODO]                                │
├─────────────────────────────────────────────────────────────┤
│  API Routes                                                  │
│  ├── /api/sessions      [TODO]                               │
│  ├── /api/anchors       [TODO]                               │
│  ├── /api/timelines     [TODO]                               │
│  ├── /api/chat          ✅                                   │
│  └── /api/tutor         ✅                                   │
├─────────────────────────────────────────────────────────────┤
│  Services (内置)                                             │
│  ├── llm-service        ✅ 直接调用 DashScope                │
│  ├── tingwu-service     ⚠️ 需改为直接集成                    │
│  ├── anchor-service     ✅ 改为调用 API                      │
│  ├── memory-service     ✅ 改为调用 API                      │
│  └── notebook-service   ✅ 可选增强                          │
├─────────────────────────────────────────────────────────────┤
│  Database                                                    │
│  └── SQLite/PostgreSQL  [TODO]                               │
└─────────────────────────────────────────────────────────────┘
         │                    │                    │
         ▼                    ▼                    ▼
    ┌─────────┐         ┌─────────┐         ┌─────────┐
    │DashScope│         │Open     │         │ OSS     │
    │(LLM)    │         │Notebook │         │(Audio)  │
    │ 必需    │         │ 可选    │         │ TODO    │
    └─────────┘         └─────────┘         └─────────┘
```

### 4.2 数据模型

```typescript
// 统一数据模型
interface Session {
  id: string;
  studentId: string;
  subject: string;
  teacher: string;
  date: string;
  status: 'recording' | 'completed';
  audioUrl?: string;
  createdAt: Date;
  updatedAt: Date;
}

interface Anchor {
  id: string;
  sessionId: string;
  studentId: string;
  timestamp: number;
  type: 'confusion' | 'important' | 'question';
  status: 'active' | 'cancelled' | 'resolved';
  note?: string;
  createdAt: Date;
}

interface Segment {
  id: string;
  sessionId: string;
  text: string;
  startMs: number;
  endMs: number;
  speakerId?: string;
  confidence: number;
  type: 'lecture' | 'qa' | 'exercise';
}

interface Timeline {
  id: string;
  sessionId: string;
  segments: Segment[];
  anchors: Anchor[];
  topics: Topic[];
  createdAt: Date;
}
```

---

## 五、快速修复清单

以下修复可立即执行，不涉及架构变更：

### 5.1 修复 Mock 数据分离

```bash
# 创建 fixtures 目录
mkdir -p src/fixtures
# 移动 MOCK_SEGMENTS 到独立文件
```

### 5.2 添加配置验证

```typescript
// src/lib/config.ts
export function validateConfig() {
  const required = ['DASHSCOPE_API_KEY'];
  const missing = required.filter(key => !process.env[key]);
  if (missing.length > 0) {
    console.warn(`Missing env vars: ${missing.join(', ')}`);
  }
}
```

### 5.3 统一类型定义

```typescript
// src/types/index.ts
export type { Anchor, AnchorType } from '@/lib/services/anchor-service';
export type { ClassTimeline, TimelineSegment } from '@/lib/services/memory-service';
// ... 统一导出
```

---

## 六、下一步行动

1. **立即执行**: 修复 P0 问题中的 T01-T03
2. **本周完成**: P1 问题中的 T04-T06
3. **下周优化**: P2/P3 问题

---

> 文档版本: v1.0
> 维护者: MeetMind 团队
